# Example values.yaml

# Expects a secret `cluster-secrets` with the following entries:
# DATABASE_URL=postgresql://postgres:postgrespassword@postgresql:5432/postgres
# REDIS_NODES=redis://:redispassword@redis-master:6379/0
# PRIVATE_KEY=
#
# See https://kubernetes.io/docs/tasks/configmap-secret/managing-secret-using-kubectl/#create-a-secret

# Also expects a secret `ghcr-secret` (for `imagePullSecrets`) if using a private image repo.
# See https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/

global:
  redis:
    # Change this to a secure password.
    password: redispassword

redis-store:
  enabled: true
  redis:
    fullnameOverride: redis
    master:
      resources:
        limits:
          memory: "30Gi"
        requests:
          memory: "30Gi"
    # Recommended to place on a non-worker machine.
    # nodeSelector: {}
    # tolerations: []
    # affinity: {}

cpu-node:
  enabled: true
  replicaCount: 1
  # Workaround to allow circuit artifacts volume to be writable, since hostPath volumes are only
  # writable by root.
  securityContext:
    runAsUser: 0
    runAsGroup: 0
    fsGroup: 0
  # Mount circuit artifacts from the host so that the artifacts are preserved across restarts.
  volumes:
    - name: sp1-circuits
      hostPath:
        # Change this to any desired path on the host where circuit artifacts should be stored.
        path: /tmp/.sp1/circuits
        type: DirectoryOrCreate
  volumeMounts:
    - name: sp1-circuits
      mountPath: /root/.sp1/circuits
  extraEnv:
    NODE_COORDINATOR_RPC: http://coordinator-grpc:50051
    # WORKER_MAX_WEIGHT_OVERRIDE is auto-detected from the pod's cgroup
    # memory limit; only set it if the auto-detect picks the wrong value.
    NODE_ARTIFACT_STORE: redis
    NODE_REDIS_NODES:
      valueFrom:
        secretKeyRef:
          name: cluster-secrets
          key: REDIS_NODES
  resources:
    # At least 96 GB of RAM to fit Controller + PlonkWrap concurrently.
    requests:
      cpu: 4
      memory: 96Gi
    limits:
      cpu: 8
      memory: 96Gi
  image:
    tag: "base-v2.2.1"
  imagePullSecrets:
    - name: ghcr-secret
  # Recommended to place on a non-GPU machine with a powerful CPU.
  # nodeSelector: {}
  # tolerations: []
  # affinity: {}

gpu-node:
  enabled: true
  replicaCount: 2
  extraEnv:
    NODE_COORDINATOR_RPC: http://coordinator-grpc:50051
    # WORKER_MAX_WEIGHT_OVERRIDE is auto-detected from the pod's cgroup
    # memory limit; only set it if the auto-detect picks the wrong value.
    NODE_ARTIFACT_STORE: redis
    NODE_REDIS_NODES:
      valueFrom:
        secretKeyRef:
          name: cluster-secrets
          key: REDIS_NODES
  resources:
    requests:
      # Each GPU pod should have at least 16GB RAM and as many cores as possible.
      # Also, only certain GPUs with >=24GB RAM are supported.
      nvidia.com/gpu: 1
      # cpu: 32.0
      # memory: 24Gi
    limits:
      nvidia.com/gpu: 1
      # cpu: 32.0
      # memory: 24Gi
  image:
    tag: "node-gpu-v2.2.1"
  imagePullSecrets:
    - name: ghcr-secret

api:
  enabled: true
  extraEnv:
    API_GRPC_ADDR: 0.0.0.0:50051
    API_HTTP_ADDR: 0.0.0.0:3000
    API_AUTO_MIGRATE: "true"
    API_DATABASE_URL:
      valueFrom:
        secretKeyRef:
          name: cluster-secrets
          key: DATABASE_URL
  replicaCount: 1
  image:
    tag: "base-v2.2.1"
  imagePullSecrets:
    - name: ghcr-secret
  # Recommended to place on a non-worker machine.
  # nodeSelector: {}
  # tolerations: []
  # affinity: {}

coordinator:
  enabled: true
  extraEnv:
    COORDINATOR_CLUSTER_RPC: http://api-grpc:50051
    COORDINATOR_METRICS_ADDR: 0.0.0.0:9090
    COORDINATOR_ADDR: 0.0.0.0:50051
  image:
    tag: "base-v2.2.1"
  imagePullSecrets:
    - name: ghcr-secret
  # Recommended to place on a non-worker machine.
  # nodeSelector: {}
  # tolerations: []
  # affinity: {}

postgresql:
  enabled: true
  fullnameOverride: postgresql
  image:
    repository: bitnami/postgresql
  auth:
    # Change this to a secure password.
    postgresPassword: postgrespassword
  primary:
    # Optionally, enable persistence to retain proof request data across restarts.
    persistence:
      enabled: false
    # Recommended to place on a non-worker machine.
    # nodeSelector: {}
    # tolerations: []
    # affinity: {}
  backup:
    enabled: false
  service:
    type: ClusterIP
    port: 5432

fulfiller:
  # The fulfiller is only necessary for fulfilling proofs on the Prover Network.
  enabled: false
  extraEnv:
    FULFILLER_CLUSTER_RPC: http://coordinator-grpc:50051
    FULFILLER_LOG_FORMAT: Pretty
    FULFILLER_CLUSTER_ARTIFACT_STORE: redis
    # Ensure these five env vars are set correctly.
    FULFILLER_RPC_GRPC_ADDR: https://rpc.sepolia.succinct.xyz
    FULFILLER_VERSION: sp1-v6.0.0
    FULFILLER_DOMAIN: SPN_SEPOLIA_V1_DOMAIN
    FULFILLER_CLUSTER_REDIS_NODES:
      valueFrom:
        secretKeyRef:
          name: cluster-secrets
          key: REDIS_NODES
    FULFILLER_SP1_PRIVATE_KEY:
      valueFrom:
        secretKeyRef:
          name: cluster-secrets
          key: PRIVATE_KEY
  image:
    tag: "base-v2.2.1"
  imagePullSecrets:
    - name: ghcr-secret
  # Recommended to place on a non-worker machine.
  # nodeSelector: {}
  # tolerations: []
  # affinity: {}

network-gateway:
  # The gateway terminates the SDK's ProverNetwork.Base + ArtifactStore RPCs
  # and translates them into ClusterService calls — set enabled=true if you
  # want to drive this cluster with sp1-sdk's NetworkProver.
  enabled: false
  extraEnv:
    GATEWAY_GRPC_ADDR: 0.0.0.0:50061
    GATEWAY_HTTP_ADDR: 0.0.0.0:8081
    # MUST be set to a URL reachable BY THE SDK CLIENT, not by other pods.
    # Example: https://gw.example.com
    GATEWAY_PUBLIC_HTTP_URL: http://network-gateway-http:8081
    GATEWAY_CLUSTER_RPC: http://api-grpc:50051
    GATEWAY_ARTIFACT_STORE: redis
    GATEWAY_REDIS_NODES:
      valueFrom:
        secretKeyRef:
          name: cluster-secrets
          key: REDIS_NODES
    # `memory` (default) keeps registered programs in process memory; switch
    # to `fs` with a PVC mounted at /var/lib/sp1-gateway/programs for durable
    # multi-restart behavior.
    GATEWAY_PROGRAM_STORE: memory
    # GATEWAY_AUTH_MODE: verify
    # GATEWAY_AUTH_ALLOWLIST: 0xabc...,0xdef...
  image:
    tag: "base-v2.2.1"
  imagePullSecrets:
    - name: ghcr-secret
  # nodeSelector: {}
  # tolerations: []
  # affinity: {}

bidder:
  # The bidder is only necessary for bidding on proofs on the Prover Network.
  enabled: false
  extraEnv:
    BIDDER_LOG_FORMAT: Pretty
    # Ensure these env vars are set correctly.
    BIDDER_RPC_GRPC_ADDR: https://rpc.sepolia.succinct.xyz
    BIDDER_VERSION: sp1-v6.0.0
    BIDDER_DOMAIN: SPN_SEPOLIA_V1_DOMAIN
    BIDDER_SP1_PRIVATE_KEY:
      valueFrom:
        secretKeyRef:
          name: cluster-secrets
          key: PRIVATE_KEY
    BIDDER_THROUGHPUT_MGAS: 1
    BIDDER_MAX_CONCURRENT_PROOFS: 1
    BIDDER_BID_AMOUNT: 1
  image:
    tag: "base-v2.2.1"
  imagePullSecrets:
    - name: ghcr-secret
  # Recommended to place on a non-worker machine.
  # nodeSelector: {}
  # tolerations: []
  # affinity: {}
